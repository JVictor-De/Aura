// Basic smoke test for Zoe Portal

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Portal smoke test', (WidgetTester tester) async {
    // Placeholder — portal requires async DI init
    expect(1 + 1, equals(2));
  });
}
